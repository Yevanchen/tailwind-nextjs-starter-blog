# 迁移核对记录

2026-09-17 从原公开 Firestore `bloglist` 集合导出 36 条记录，完整分页返回、没有下一页。原始导出仅保留于本地工作目录，不提交凭据或数据库配置。

- 导入 36 篇 Markdown，原有 3 篇 MDX 保留（含原隐藏文件 `.mdx`）；总计 39 个文章地址。
- 原隐藏测试文章文件名为空，恢复为 `legacy-test.mdx`，保留原文 `11`。
- 同标题 Clawdbot 工作流文章保留两个历史地址；仓库副本标记 `unlisted`，首页/搜索/RSS 只展示主文章，共 38 个归档条目，避免重复。
- 35 个有效旧 slug 完整保留，包括中文、空格、前后连字符。
- 《日拱一卒》原 slug 为空，修复为 `daily-progress`，此前没有可保留的有效详情链接。
- 2 篇导入文章以及原仓库测试文章的正文为空。保留原记录，页面明确说明为空，不补写内容。
- 旧教程内一处访问令牌格式示例替换为 `YOUR_GITHUB_TOKEN`；这是正文唯一主动变更。
- `data/migration-manifest.json` 保存原始正文 SHA-256；脱敏文章另存迁移后 SHA-256，测试对逐篇正文和路径核验。
- 未删除云端 Firebase 数据、未更改其权限、未停止 Zeabur 服务。

迁移核验脚本 `node --test tests/migration-audit.mjs` 用于首次验收，逐篇对比正文校验和；它不参与后续日常 CI，因此通过 PR 编辑或删除旧文章无需更新历史清单。清单作为只读审计记录保留。
